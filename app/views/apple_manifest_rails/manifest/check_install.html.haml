- if @checker.installable?(@udid)
  = javascript_tag "window.location.href = '#{@itms_url}'"
  Your application is now downloading and will install momentarily. Please close this window and return to the iOS home screen.
- else
  Your device is not provisioned and so cannot install.
  Your UDID: #{@udid}